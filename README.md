# GatewayStack – Web UI

Minimal frontend used to trigger backend flows and emit structured logs.  
No business logic. No response rendering.

---

## Requirements

- Node.js **18+**
- npm **9+**

Verify versions:

```bash
node -v
npm -v
```
